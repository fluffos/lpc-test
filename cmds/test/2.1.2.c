// 示例：2.1.2
int main(object me, string arg)
{
    int a = 16, b = 'a', c = 0xF;

    printf("a 十进制为%d，八进制为%o，十六进制为%x\n", a, a, a);
    printf("b 十进制为%d，ASCII字符为%c\n", b, b);
    printf("c 十进制为%d，十六进制为%x或%X\n", c, c, c);

    return 1;
}
